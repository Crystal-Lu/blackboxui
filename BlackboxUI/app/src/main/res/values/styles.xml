<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryDark">#2E2F33</item>
        <item name="colorAccent">@color/hintPink</item>
        <item name="colorControlHighlight">@color/grey</item>
        <item name="fontFamily">@font/bahnschrift</item>
    </style>

    <style name="Type">
        <item name="android:textColor">@color/white</item>
        <item name="font">@font/bahnschrift</item>
    </style>


</resources>
